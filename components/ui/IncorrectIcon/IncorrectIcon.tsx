import React from 'react';

interface IncorrectIconProps {
  isDark: boolean;
}

const IncorrectIcon: React.FC<IncorrectIconProps> = ({ isDark }) => {
  return (
    <div>
        
    </div>
  );
};

export default IncorrectIcon;